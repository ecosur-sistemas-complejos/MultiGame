import mx.ecosur.multigame.easyb.SharedBoardHelper;

scenario "first-token", {
	when "the-first-player positions a token", {
		
	}
	then "it must be in the center of the board", {	
		
	}
}

scenario "subsequent-token", {
	when "any player positions a token", {
		
	}
	
	and "that player is not the first player", {
		
	}
	
	then "that token can be anywhere excepton upon an existing token", {
		
	}
}

